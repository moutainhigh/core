spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: spring-cloud-config-server-9091
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka