spring:
  application:
    name: user-service
  kafka:
    bootstrap-servers: 192.168.216.128:9092
    consumer:
      group-id: user-service
  cloud:
    bus:
      enabled: true
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: refresh


